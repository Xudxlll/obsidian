# 智研投顾 Agent 项目汇报 PPT 讲解稿

## 第 1 页：封面

各位评委老师好，我是徐嘉皓，来自“不写代码队”。我本次汇报的项目是“智研投顾 Agent”，它是一个面向国内行业、股票以及 ETF / 指数研究场景的金融研究工作流智能体。本项目基于百度千帆工作流 Agent 搭建，核心路线是一个主 Agent，加上百度搜索、广发证券 MCP 和工作流节点编排。接下来我会从项目背景、系统架构、核心工作流、关键节点设计和后续优化几个方面进行介绍。

## 第 2 页：项目背景

普通大模型本身已经可以回答很多金融相关问题，但在投资研究场景中，仅依赖大模型自由回答会有明显局限。金融问题非常依赖最新市场信息、政策动态、行业新闻以及结构化指标，如果大模型只依靠已有记忆，就容易出现信息滞后。除此之外，用户的问题类型也比较混杂，可能是行业分析、股票查询、ETF 对比，也可能只是普通聊天。因此，本项目的出发点是把开放式聊天回答改造成一个更可控的金融研究工作流。

## 第 3 页：要解决的核心问题

本项目主要解决三个核心问题。第一，是意图区分问题，也就是判断用户到底是在问行业、股票、ETF / 指数、通用财经事实，还是日常聊天。第二，是信息组合问题，也就是如何把百度搜索结果、广发证券 MCP 的结构化数据和大模型的分析能力组合起来。第三，是合规控制问题，因为金融场景不能随意输出具体股票推荐，也不能给出确定性的买卖建议。

## 第 4 页：项目目标

本项目第一阶段的目标不是做一个万能投资顾问，而是先搭建一个可运行、可调试、可扩展的 MVP。它的核心路线是一个主 Agent，加上百度搜索、广发证券 MCP 和工作流。最终希望实现的效果是，当用户提出国内行业、股票、ETF / 指数等金融研究问题时，系统能够自动分流、检索信息、调用指标，并生成相对完整的研究分析报告。

## 第 5 页：核心能力范围

目前这个 Agent 支持的能力主要包括国内行业推荐、国内行业分析、多个行业对比、国内股票分析、ETF / 指数查询分析和对比，以及其他财经、公司、市场热点问题的联网回答。同时，对于普通日常聊天问题，系统会直接交给普通大模型回答。需要特别说明的是，股票分析可以做，但具体股票推荐不会做，这是项目中非常重要的合规边界。

## 第 6 页：为什么选择工作流 Agent

我选择工作流 Agent，是因为金融研究本身不是一个单步问答，而是一个多阶段流程。一个完整回答通常要先理解用户问题，再判断问题类型，然后搜索最新资料，获取结构化指标，清洗和整理材料，最后生成研究报告并加入风险提示。相比只用单个聊天式大模型，工作流 Agent 可以把这些步骤拆开，让每个环节更可控、更容易调试。

## 第 7 页：工作流 Agent 的优势

工作流 Agent 对这个项目的价值主要体现在四个方面。第一是路径可控，不同类型的问题会进入不同分支。第二是节点可调试，每个节点都能查看输入和输出，方便定位问题。第三是结果可复用，例如 Query 改写、搜索、资料整理和指标分析都可以模块化复用。第四是风险可控制，特别是在股票推荐和最终报告输出的位置，可以加入合规限制。

## 第 8 页：系统节点规模

当前这个 Agent 的整体规模已经比较完整，包括 27 个大模型节点、10 个 MSAP 节点、3 个分支器节点、2 个代码节点、6 个循环节点、1 个参数聚合节点、2 个信息收集节点和 1 个消息节点。这一页主要展示系统规模，说明它已经不是一个简单的单节点问答，而是一个由多类节点共同组成的工作流系统。

## 第 9 页：第一层总分流设计

系统开始后，第一步是总分类大模型节点。这个节点只输出 1、2、3、4 四类结果。输出 1 表示国内行业、股票分析或 ETF / 指数问题，会进入国内金融主线。输出 2 表示股票推荐类问题，会进入合规拦截。输出 3 表示其他需要联网的金融、公司、行业、商业、经济或时事问题，会进入百度搜索加大模型总结。输出 4 则是日常聊天或不需要联网的问题，直接由普通大模型回答。

## 第 10 页：股票推荐与事实查询的边界

这里我专门区分了股票推荐和股票事实查询。比如用户说“推荐几只新能源股票”，这属于股票推荐，会进入分类 2 并被合规拦截。但如果用户只是问“贵州茅台的股票代码是什么”，这只是事实查询，会进入分类 3，通过联网搜索回答。这样设计的目的，是避免把普通事实查询误判成高风险问题，同时也避免系统真的去推荐具体股票。

## 第 11 页：第二层国内金融意图识别

当第一层总分类输出 1 后，问题会进入国内金融主线。这里还会再经过一个金融意图识别大模型，把问题进一步分成五类：ETF 指数分析、股票分析、行业推荐、行业对比和行业分析。这一步的作用是把大的国内金融问题继续拆成更具体的执行路线，让后续工作流能够精准调用对应的节点。

## 第 12 页：国内金融意图识别规则

这一页展示的是第二层金融意图识别的判断规则。比如用户提到 ETF、指数或指数基金，就进入 ETF 指数分析；提到具体股票名称、股票代码、个股分析或个股对比，就进入股票分析；询问哪些行业值得关注，就进入行业推荐；比较多个行业，就进入行业对比；指定某个行业问现状、前景和风险，就进入行业分析。这样可以让分类结果更加稳定。

## 第 13 页：完整工作流树状图

这一页是整个系统的总览图。用户输入后，先进入总分类大模型，然后被分到四条一级路线。其中分类 1 会继续进入金融意图识别，并拆成 ETF 指数分析、股票分析、行业推荐、行业对比和行业分析五条主线。分类 2 直接进入股票推荐拦截，分类 3 进入通用联网财经问答，分类 4 则进入日常聊天。这种树状结构的核心价值，是先把高风险和高复杂度问题分开，再让不同问题走不同链路。

## 第 14 页：行业推荐主线流程

行业推荐主线处理的是用户没有指定单一行业，而是想知道哪些行业值得关注的情况。系统会先接收用户问题，再进行 Query 改写，把问题拆成行业资料搜索和股票线索搜索两条并行链路。一条链路搜索行业机会、政策、热点和景气度，并做资料整理去重；另一条链路搜索代表股票，提取股票代码并调用 MCP 查询指标。最后，系统把行业机会和相关股票指标观察合并成最终行业推荐报告。这里的股票只是作为行业代表公司观察，不构成买卖建议。

## 第 15 页：行业推荐主线价值

行业推荐主线的关键价值在于，它不是直接依赖大模型记忆来推荐行业，而是先通过搜索获取最新资料。搜索内容会覆盖行业机会、政策方向、市场热点和行业景气度。同时，相关股票只是作为行业代表公司的指标观察，不作为具体投资建议。最终报告会以“行业观点 + 相关股票指标观察”的形式呈现，让用户看到行业层面的机会和对应的结构化指标。

## 第 16 页：行业分析主线流程

行业分析主线处理的是用户已经指定某一个行业，希望分析这个行业现状、前景和风险的情况。系统会先识别目标行业，然后并行形成两条证据链。一条是行业研究材料链路，包括改写分析 Query、搜索政策、景气度、产业链和风险信息，并进行整理去重；另一条是代表股票指标链路，包括搜索行业相关 A 股代表公司、提取股票代码并调用 MCP 获取指标。最后，系统将行业材料和股票指标汇总成最终行业分析报告。

## 第 17 页：行业分析输出重点

行业分析报告重点呈现六类内容：行业现状、政策和市场热点、产业链与竞争格局、主要机会和风险、相关股票指标观察，以及风险提示和免责声明。这里强调的是，行业分析不是简单把搜索结果拼接起来，而是围绕一个指定行业形成相对完整的研究报告。

## 第 18 页：行业对比主线流程

行业对比主线的关键不是直接比较，而是先把多个行业拆开分别分析。比如用户问人工智能和新能源哪个更值得关注，系统会先提取行业列表，然后分别搜索每个行业的资料，分别整理每个行业的分析结果，再汇总代表股票指标。最后再从政策、景气度、产业链、估值和风险等维度进行横向比较，并输出对比结论。

## 第 19 页：行业对比设计原则

行业对比主线遵循三个原则。第一，先分别分析，再横向对比，避免没有依据地直接判断。第二，对比维度要尽量统一，不能 A 行业讲政策，B 行业讲股票，C 行业讲市场情绪。第三，不输出确定性投资结论，只提供研究分析和风险提示。这样可以让行业对比更稳定，也更符合金融合规要求。

## 第 20 页：股票分析主线

股票分析主线适用于用户分析某只股票、查询某只股票的基本面估值风险，或者对比多个个股。基本流程是先识别股票名称或股票代码，然后通过百度搜索获取公司和股票相关资料，再调用广发证券财务对比分析 MCP 获取指标，最后由大模型整理基本面、估值和风险，输出个股研究分析报告。这里仍然是分析，不是推荐买入或卖出。

## 第 21 页：ETF / 指数分析主线

ETF / 指数分析主线适用于用户分析某个指数或 ETF、对比多个 ETF 或指数，或者查询主题 ETF、行业 ETF、宽基指数等问题。这个主线主要使用热门 ETF 榜单 MCP、沪深指数估值分析 MCP，并结合百度搜索获取市场背景。最终输出必须是研究分析，不能给出买入、卖出、加仓或清仓建议。

## 第 22 页：股票推荐类问题的合规拦截

如果用户明确要求推荐具体股票、股票名单、投资标的或股票池，系统会进入分类 2，也就是合规拦截路线。这个路线不会继续搜索并生成股票推荐，而是直接输出合规提示，并引导用户改问行业分析、股票分析、风险分析或研究路径。例如用户问“推荐几只新能源股票”，系统会提示不支持推荐具体股票，可以改为询问新能源行业的产业链、风险和代表公司。

## 第 23 页：Query 改写设计

用户原始问题往往不适合直接搜索。比如“新能源行业怎么样”这个问题太口语化，也太宽泛。Query 改写节点会把它拆成更适合搜索的字段，比如行业分析搜索 Query 和股票线索搜索 Query。这样后续搜索节点就能分别查行业政策、景气度、产业链、风险，以及行业相关 A 股龙头公司和股票代码。

## 第 24 页：Query 改写的价值

Query 改写节点的价值主要有四点。第一，它把行业资料搜索和股票线索搜索分开。第二，它提高搜索结果的相关性。第三，它为后续百度搜索节点、循环节点、代码节点和 MCP 节点提供结构化输入。第四，它让后续工作流不再直接依赖用户原始表达，而是依赖更稳定、更适合检索的 query。

## 第 25 页：百度搜索节点的作用

百度搜索节点解决的是最新信息来源的问题。它主要用于获取行业新闻、政策动态、市场热点、行业景气度、产业链变化、风险事件，以及相关上市公司和股票代码线索。对于金融研究场景来说，搜索节点非常重要，因为很多问题都需要结合最新市场信息，而不是只依赖大模型已有知识。

## 第 26 页：搜索结果整理逻辑

搜索结果不会直接进入最终报告，而是先交给大模型资料整理节点处理。这个节点主要负责去重、合并同类信息、删除无关内容、自然段重述，并保留关键事实和时间线索。它的作用是把原本杂乱的搜索结果，整理成可以用于报告生成的干净材料，减少最终报告中的重复和噪声。

## 第 27 页：广发证券 MCP 数据源

本项目使用了广发证券提供的三个 MCP。第一个是热门 ETF 榜单 MCP，用于获取热门 ETF 榜单信息。第二个是沪深指数估值分析 MCP，用于获取沪深指数估值相关信息。第三个是财务对比分析 MCP，用于获取股票财务和估值指标，支持个股分析、行业代表股票观察和财务对比。这些 MCP 提供的是结构化金融数据，是项目中非常关键的数据来源。

## 第 28 页：股票代码提取与格式控制

财经 MCP 对股票代码格式有要求，所以系统必须先从搜索资料中准确提取股票代码。这里我规定输出格式为数组字符串，例如 SZ300308、SZ002594、SH600519。规则包括只提取中国 A 股，最多提取 3 个，必须是 SZ 或 SH 加 6 位数字，不能输出港股、美股、基金、ETF 或指数代码。如果无法判断前缀，就不输出。这样可以减少后续 MCP 节点报错。

## 第 29 页：代码节点的作用

代码节点主要用于把上游大模型输出的 JSON 拆成后续节点可以使用的变量。比如上游输出一个 query_rewrite_result，代码节点会判断它是字符串还是对象，如果是字符串就先 json.loads 解析，然后再返回 industry_search_query 和 stock_discovery_query。这样下游节点就可以直接引用这些字段，而不是继续处理一整段 JSON。

## 第 30 页：代码节点与循环节点的衔接

如果后续要进入循环节点，代码节点还可以把多个 query 组装成一个数组。例如把 industry_search_query 和 stock_discovery_query 放进 query_list 里。这里有一个关键点：千帆代码节点最后必须返回字典，只有返回字典后，下游节点才能引用对应输出项。数组则可以作为循环节点输入，让循环节点逐项并行处理。

## 第 31 页：循环节点并行处理逻辑

本项目中的循环节点主要用于并行运行。整体思路是上游先输出一个 array 数组，循环节点接收后，同时处理数组中的每个元素。每一轮会输出一个局部结果，最后再把多个局部结果累加起来，交给最终汇总节点。这样可以避免重复搭建大量相似节点，也能提高整个工作流的运行效率。

## 第 32 页：循环节点适用场景

循环节点适合用在多个相似任务并行处理的场景。比如行业对比中，对多个行业分别搜索和整理；对多个搜索 query 批量执行百度搜索；对多个候选对象逐个做资料整理；或者把多个结果汇总成统一结构。它的优势是避免重复节点、保持每轮处理逻辑一致，并缩短整体运行时间。

## 第 33 页：Prompt 设计分类

这个项目不是只依赖一个大 Prompt，而是为不同节点设计不同 Prompt。比如意图识别 Prompt 只输出分类结果，不解释；Query 改写 Prompt 负责把用户问题拆成搜索 query；资料整理 Prompt 只做去重和自然段重述，不做最终结论；代码提取 Prompt 严格输出 Array<String>；指标整理 Prompt 把 MCP JSON 转成可读说明；通用联网回答 Prompt 则根据用户问题复杂度决定简答或深度分析。

## 第 34 页：Prompt 设计原则

Prompt 设计主要遵循三个原则。第一，每个节点只做一件事，避免一个节点同时分类、搜索、总结和推荐。第二，输出格式要严格，尤其是分类节点、代码提取节点和 Query 改写节点。第三，最终报告节点只负责排版、衔接和合规输出，不随意压缩上游内容。这样做可以提高工作流稳定性，也方便定位某个节点的问题。

## 第 35 页：合规风控设计

合规风控是本项目的重点之一。系统输出时不能承诺收益，不能使用“稳赚”“必涨”“保证收益”“无风险”等绝对化表达，不能直接推荐用户买入或卖出股票、基金或 ETF，也不能给出具体买入价、卖出价、加仓或清仓指令。如果资料不足，还要主动降低结论确定性。最终回答也必须包含风险提示和免责声明。

## 第 36 页：典型测试案例：国内金融主线

这一页展示的是国内金融主线的典型测试案例。比如“现在 A 股有哪些行业值得关注”应该进入行业推荐；“帮我分析半导体行业”应该进入行业分析；“人工智能和新能源哪个更值得关注”应该进入行业对比；“帮我分析中际旭创”应该进入股票分析；“半导体 ETF 和人工智能 ETF 哪个更值得关注”应该进入 ETF 指数分析。这些案例主要验证分类链路是否能正确分流。

## 第 37 页：典型测试案例：拦截、联网与聊天

这一页展示的是另外三类分流测试。比如“推荐几只新能源股票”会进入分类 2，也就是合规拦截。“贵州茅台的股票代码是什么”属于事实查询，会进入分类 3。“英伟达最近有什么新闻”也需要联网搜索，因此进入分类 3。而“你好，帮我解释一下 Agent 是什么”不需要联网，可以进入分类 4，由普通大模型直接回答。

## 第 38 页：项目亮点总结

本项目的亮点主要有七点。第一，把复杂金融问题拆成了可控的多层分流工作流。第二，支持行业、股票、ETF / 指数和通用联网财经问题。第三，使用百度搜索获取最新资料，减少纯模型记忆带来的滞后。第四，使用广发证券 MCP 获取结构化指标。第五，通过 Query 改写、代码节点和循环节点提高运行效率。第六，通过多节点 Prompt 分工提高输出稳定性。第七，在股票推荐和最终输出中加入了合规控制。

## 第 39 页：当前不足

目前项目也还有一些不足。首先，搜索结果质量会直接影响最终回答质量。其次，部分金融事实仍然需要以交易所公告、基金公告或公司公告为准。第三，ETF / 指数指标获取链路还可以继续标准化。第四，多轮对话中的用户偏好和上下文记忆还可以增强。第五，最终输出格式也可以进一步做成更稳定的报告模板。

## 第 40 页：后续优化方向

后续我计划从几个方向继续优化。第一，加入知识库，用来沉淀行业研究框架和合规模板。第二，把当前单一主 Agent 拆分为多智能体协同架构。第三，增强 ETF / 指数、基金和持仓诊断能力。第四，增加最终风控检查节点。第五，增加测试集，系统评估分类准确率和回答质量。

## 第 41 页：后续多智能体升级设想

后续这个项目可以从一个主 Agent 升级为多智能体协同架构。规划主 Agent 负责任务拆解和结果汇总，下面可以拆出宏观策略 Agent、行业研究 Agent、数据检索 Agent、基金 / ETF 分析 Agent、持仓诊断 Agent、风险合规 Agent 和报告撰写 Agent。这样每个子 Agent 负责一个专业模块，整体系统的专业性和扩展性都会更强。

## 第 42 页：结尾页

最后总结一下，本项目完成了一个以国内金融研究为核心的工作流 Agent 原型。它通过多层意图识别、百度搜索、广发证券 MCP、循环节点、代码节点和多类 Prompt，把用户的金融问题转化为可控、可追踪、合规的研究报告生成流程。它的核心价值可以概括为一句话：从“聊天回答”升级为“可控的金融研究工作流”。我的汇报到这里结束，谢谢各位老师。
