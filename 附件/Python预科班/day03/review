一 运算符
1 算数运算符 +  -  *  /除法(小数商)  //整数商  %取余  **幂运算
2 增强运算符 += -= *= /= //= %= **= 一行代码内实现数据读取，赋值，修改的操作。主要用于循环当中
            a = 7; a %= 2 --> a除以2的余数重新赋值给a=1
3 比较运算符 >  >=  <  <=   ==等于   !=不等于
4 逻辑运算符  and与  并且的关系  一假俱假; or或  或者的关系  一真俱真;  not 非 取反
运算符的优先级 算数(有括号先算括号，没括号幂运算，然后先乘除后加减) --> 比较运算符 --> 逻辑运算符(not-->and-->or)

二 语句
1 行:一个逻辑行在一个物理行;
  换行:显示换行 \ 隐式行换:() [] {}
2 选择语句:让程序根据条件选择性的执行语句
     条件:带有判断性质的表达式，返回布尔类型值: True(条件成立/满足)   False(条件不成立/不满足)
第一种结构:if 条件:基于一个条件的判断，条件成立执行缩进的代码
第二种结构:if 条件...else：基于一个条件的两种结果的判断，互斥的关系(二选一)
第三种结构：if-elif-else: 互斥的关系（多选一，多路分支）多个条件自上而下判断，一旦满足，则执行缩进的代码，后面的条件不再判断
第四种结构:多个if:条件之间是独立并列的结构，互不影响。自上而下逐一判断每个条件，只要条件满足，即执行缩进的代码