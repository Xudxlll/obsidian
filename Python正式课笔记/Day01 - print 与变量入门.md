## 今天学了什么

> [!info] 今天学了什么
> - 认识代码文件中注释、三引号说明、可执行语句之间的区别。
> - 使用 `print()` 输出内容，并观察默认换行与 `end` 参数的效果。
> - 理解变量命名规则、赋值语句，以及变量如何保存姓名、年龄等数据。
> - 学习 `%` 占位符格式化和 f-string，把变量嵌入字符串输出。

## 抓主线

> [!tip] 抓主线
> 1. 先用 `print()` 让程序把结果显示出来。
> 2. 再用变量给数据起名字，让后续代码可以复用数据。
> 3. 最后把变量和字符串组织成更完整的输出句子。

## 课堂代码合集

### Day01/01_print.py

```python
"""
注释 + 代码正文 + 缩进
注释:
   给人看的 解释代码
   注释的内容不会运行
   单行 #
   多行 三引号

"""
# print 输出函数 把括号内的内容输出到控制台,默认带换行
# 调试阶段使用 内置的(拿过来直接用)
print("Hello World")
print("你好 世界")
print("你好 世界", end="")
print("你好 世界")
'''
编译 程序运行前 编译器 C/C++ 快 开发效率低
解释 程序运行时 解释器 JavaScript 慢 开发效率高

python为了提高执行效率,运行时采用了编译方法

[源代码 --> 编译器 --> 中间语言(.pyc)][第一次运行] --> 解释器 --> 机器码
'''
```

> [!note] 这份文件实际讲了什么
> - 文件说明/题目写的是：注释 + 代码正文 + 缩进；注释:；给人看的 解释代码；注释的内容不会运行；单行 #。
> - 后续说明/题目还包括：编译 程序运行前 编译器 C/C++ 快 开发效率低；解释 程序运行时 解释器 JavaScript 慢 开发效率高；python为了提高执行效率,运行时采用了编译方法。

> [!example] 本文件出现的新代码 / 函数 / 方法
> - `print()`：第一次重点学习输出函数，用它把内容显示到控制台。

> [!abstract] 代码逻辑怎么走
> - 这份代码主要是顺序执行：先准备数据，再调用函数或输出结果。

> [!warning] 需要注意的点
> - 读这份代码时，先看变量如何变化，再看最终输出。

> [!success] 举一反三
> - 可以修改示例数据重新运行，观察输出如何变化。

### Day01/02_variable.py

```python
"""
变量
   数据的名字
   规范:
    不允许使用数字开头,
    使用字母,下划线开头,后面跟字母 数字 下划线
    不允许使用python保留字 for if pass ...
    符合: name1 user_name _name
    不符合: 1name print for

赋值
    过程 通过等号 = 来实现

变量 = 值   把等号右边的值赋值给左边的变量

"""
name = "小艺"
age = 18

# 打印多个变量
print(name, age)
# 我是小艺,今年18岁 %占位符 %s字符串 %d数字
print("我是%s,今年%d岁" % (name, age))
# 推荐!
print(f"我是{name},今年{age}岁")
```

> [!quote] 相关图示理解
> ![[附件/正式课/Day01/内存图1.png]]
> 这张图对应 `Day01/02_variable.py` 的知识点。复盘时重点看变量、对象和值之间的指向关系，再对照上面的代码运行过程。

> [!note] 这份文件实际讲了什么
> - 文件说明/题目写的是：变量；数据的名字；规范:；不允许使用数字开头,；使用字母,下划线开头,后面跟字母 数字 下划线。
> - 主要变量/数据名包括：`name`、`age`。

> [!example] 本文件出现的新代码 / 函数 / 方法
> - 变量赋值：用 `=` 把右侧结果保存到左侧变量名中。
> - `%` 字符串格式化：用 `%s`、`%d` 等占位后再填入变量。
> - f-string：用 `f"...{变量}..."` 把变量直接嵌入字符串。

> [!abstract] 代码逻辑怎么走
> - 通过赋值语句保存中间结果，主要变量包括：`name`、`age`。

> [!warning] 需要注意的点
> - 读这份代码时，先看变量如何变化，再看最终输出。

> [!success] 举一反三
> - 可以修改示例数据重新运行，观察输出如何变化。

### Day01/HomeWork.txt

```text
作业:
1.完成今日代码
2.提前向下预习 --> 数据(数据类型与转换 运算符)
```

> [!info] 课堂文本/作业说明
> - 作业:
> - 1.完成今日代码
> - 2.提前向下预习 --> 数据(数据类型与转换 运算符)

## 总结

> [!summary] 总结
> - **今天的核心任务**：认识代码文件中注释、三引号说明、可执行语句之间的区别。；使用 `print()` 输出内容，并观察默认换行与 `end` 参数的效果。；理解变量命名规则、赋值语句，以及变量如何保存姓名、年龄等数据。
> - **真实文件里的练习/主题**：注释 + 代码正文 + 缩进；变量。
>
> **新学代码怎么理解**
> - **print()**：把内容输出到控制台，是最常用的运行结果观察工具。
> - 变量赋值：用 `=` 把右侧结果保存到左侧变量名中。
> - **%**：`%` 格式化通过占位符把变量填进字符串，是早期常见格式化方式。
> - f-string：用 `f"...{变量}..."` 把变量直接嵌入字符串。
>
> **复盘建议**
> - 先看每份文件的三引号说明或注释，判断题目要解决什么问题。
> - 再看本文件真正新增的函数/方法，弄清楚它接收什么、返回什么、是否会修改原对象。
> - 最后把代码逻辑按“输入/准备数据 -> 分支或循环处理 -> 输出/返回结果”复述一遍。
