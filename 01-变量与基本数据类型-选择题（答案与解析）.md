# 一、Python 变量与基本数据类型 —— 选择题（答案与解析）

> 共 10 题。正确答案已加粗标注。

---

**1. 关于 Python 语言，以下说法正确的是？**

A. Python 是一种编译执行的编程语言  
B. Python 由 Guido van Rossum 于 2008 年创建  
C. **Python 是开源的、跨平台的高级编程语言** ✅  
D. Python 3.0 发布于 1991 年  

**解析：**

A ❌ Python 采用**编译和解释相结合的混合模式**，并非纯编译执行。  
B ❌ Python 由 Guido van Rossum 于 **1991 年**创建，2008 年 12 月发布的是 Python 3.0。  
C ✅ Python 是一种**开源的、跨平台的、解释性的高级编程语言**。开源意味着源代码可公开查看和修改；跨平台意味着可在 Windows、Linux、macOS 等不同系统上运行。  
D ❌ 1991 年是 Python 诞生的年份，Python 3.0 发布于 **2008 年 12 月**。

---

**2. Python 采用什么样的程序执行方式？**

A. 纯编译执行，生成机器码后运行  
B. 纯解释执行，逐行解析源代码  
C. **先将源代码编译为字节码，再由解释器执行** ✅  
D. 直接由操作系统执行源代码  

**解析：**

Python 采用的是**混合模式**：源代码首先会被编译为字节码（`.pyc` 文件），然后由 Python 解释器执行这些字节码。

A ❌ C、C++、Go 才是典型的编译型语言。  
B ❌ JavaScript、PHP 是典型的纯解释型语言。  
C ✅ 这描述的就是 Python 和 Java 采用的混合模式。  
D ❌ 源代码不能被操作系统直接执行，必须经由解释器或编译器处理。

---

**3. 以下哪个是 Python 官网版本的解释器？**

A. Jython  
B. IronPython  
C. **CPython** ✅  
D. PyPy  

**解析：**

- **CPython**：官网版本的解释器，用 C 语言开发，是最主流、最标准的 Python 实现。
- Jython：在 Java 平台上运行的 Python 解释器，可将 Python 代码编译成 Java 字节码。
- IronPython：在微软 .NET 平台上运行的解释器，可将 Python 代码编译成 .NET 字节码。
- PyPy：虽然也是 Python 解释器（用 Python 写的 JIT 实现），但笔记中没有将其列在 Python 解释器种类中。

---

**4. Python 中推荐使用什么作为缩进单位？**

A. 1 个 Tab 键  
B. 2 个空格  
C. **4 个空格** ✅  
D. 8 个空格  

**解析：**

笔记明确指出：**在 Python 中，推荐使用 4 个空格作为缩进单位，而不是 Tab 键**。这是 Python 社区（PEP 8）的推荐规范。缩进是 Python 最重要的语法规则之一，用于表示代码块的开始和结束——冒号（`:`）标记一个新的逻辑层，增加缩进表示进入下一个代码层，减少缩进表示返回上一个代码层。

---

**5. 根据 Python 变量命名规范，以下哪个是合法的变量名？**

A. `2name`  
B. `my-var`  
C. **`_count`** ✅  
D. `class`  

**解析：**

A ❌ 变量名不能以数字开头，必须以**字母或下划线**开头。  
B ❌ 变量名中不能包含短横线、斜线、反斜线等特殊符号，只能由字母、数字、下划线组成。  
C ✅ `_count` 以下划线开头，其余部分为字母，完全合法。  
D ❌ `class` 是 Python 的关键字，严禁用作变量名。

---

**6. 关于 Python 的布尔类型，以下说法正确的是？**

A. `isinstance(True, int)` 返回 `False`  
B. `5 + True` 的结果是 `5`  
C. **`True` 等价于 `1`，`False` 等价于 `0`** ✅  
D. 布尔值不能参与算术运算  

**解析：**

C ✅ 布尔型可以看作整数的子集——`True` 等价于 `1`，`False` 等价于 `0`。

A ❌ `isinstance(True, int)` 返回的是 **`True`**，因为布尔类型是整型的子类。  
B ❌ `5 + True` = `5 + 1` = **`6`**，而非 `5`。  
D ❌ 布尔值**可以**参与算术运算，在算术运算中会被隐式转换为 `1` 或 `0`（例如 `10 * False` 结果为 `0`）。

---

**7. 执行以下代码，输出结果是什么？**

```python
print((0.1 + 0.2) == 0.3)
```

A. `True`  
B. **`False`** ✅  
C. `TypeError`  
D. `None`  

**解析：**

输出结果是 **`False`**。这是因为在 Python（以及大多数编程语言）中，**浮点数的存储存在合理误差**——`0.1` 和 `0.2` 在二进制浮点表示中并不是精确值，它们相加的结果是 `0.30000000000000004`，不等于 `0.3`。

笔记中明确指出：**尽量避免对浮点数进行相等性比较操作**；但浮点数之间的大小比较通常是安全的。

---

**8. 关于原始字符串（raw string），以下哪行代码会引发 `SyntaxError`？**

A. `r"Hello\nWorld"`  
B. `r"C:\path\to\file"`  
C. **`r'C:\test\'`** ✅  
D. `r"\\\\ 表示反斜线"`  

**解析：**

原始字符串有一个**重要限制**：**不能以奇数个反斜杠（`\`）结尾**。因为最后一个 `\` 会与字符串的结束引号冲突，导致语法错误。

C ❌（语法错误） `r'C:\test\'` 以单个 `\` 结尾（奇数个），导致 `SyntaxError`。  
A ✅（合法）输出 `Hello\nWorld`，`\n` 不被转义。  
B ✅（合法）未以反斜杠结尾，正常输出 `C:\path\to\file`。  
D ✅（合法）以偶数个反斜杠（`\\\\`）结尾，正常输出 `\\\\ 表示反斜线`。

---

**9. 关于 Python 中的 `None`，以下哪个说法是\*错误\*的？**

A. `None == 0` 返回 `False`  
B. `None == ''` 返回 `False`  
C. `type(None)` 返回 `<class 'NoneType'>`  
D. **`None == False` 返回 `True`** ✅（这是错误的说法）  

**解析：**

`None` 是一个独立的常量，属于 `NoneType` 类型，表示"什么都没有"或"空值"。

D 是**错误的说法** —— `None == False` 返回的是 **`False`**。尽管在布尔上下文（如 `if None:`）中 `None` 会被视为假值，但它与布尔值 `False` 并不是同一个对象，`==` 比较也返回 `False`。

A、B、C 均为正确说法：`None` 不等于空字符串 `''`，也不等于数字 `0`；`type(None)` 确实返回 `<class 'NoneType'>`。

---

**10. 以下关于 `int()` 函数的用法，哪个输出的结果与其余三个\*不同\*？**

A. `int('1010', 2)`  
B. `int('12', 8)`  
C. `int('A', 16)`  
D. **`int('1010')`** ✅  

**解析：**

`int()` 函数可以接收第二个参数来指定进制。当忽略第二个参数时，默认以十进制解析。

- A: `int('1010', 2)` → 二进制 `1010` 转十进制 = 1×2³ + 0×2² + 1×2¹ + 0×2⁰ = **10**
- B: `int('12', 8)` → 八进制 `12` 转十进制 = 1×8¹ + 2×8⁰ = **10**
- C: `int('A', 16)` → 十六进制 `A` 转十进制 = **10**
- D: `int('1010')` → 没有指定进制，按十进制解析，结果 = **1010**（一千零十）

A、B、C 的结果都是 `10`，只有 D 的结果是 `1010`，与其余三个不同。

这道题的关键是理解：当 `int()` 没有第二个参数时，字符串按照**十进制**解析；而 `'1010'` 作为十进制数就是一千零十，而非二进制转十进制的结果 10。
