---
title: "系统代理和 TUN，到底差在哪？"
url: "https://www.bilibili.com/video/BV1PJEx6cEjj/"
bvid: "BV1PJEx6cEjj"
cid: "38921177102"
author: "飞行认知"
upload_date: "2026-06-07"
subtitle_lang: "中文"
created: "2026-06-10"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116706072596451&bvid=BV1PJEx6cEjj&cid=38921177102&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

明明开了代理，为什么浏览器能用，有些软件却不行？这期视频用最通俗的方式，带你搞懂系统代理和 TUN 模式的区别、工作原理，以及它们分别适合什么场景。小白也能看懂。

## 字幕

`00:00` 为什么你明明开了代理浏览器能用
`00:02` 有的软件却不行
`00:04` 你的流量到底是怎么被代理的
`00:06` 今天这一期我们把这件事讲清楚
`00:09` 什么是系统代理
`00:11` 什么是TUN模式
`00:12` 他们到底是怎么工作的
`00:14` 放心就算你是小白
`00:16` 看来也能搞懂
`00:18` 废话不多说
`00:19` 进入正题
`00:20` 系统代理我们先明白一点
`00:23` 系统代理并不是我们常用的代理软件
`00:26` 自己发明出来的一种能力
`00:28` 它本来就是操作系统提供的一套网络代理机制
`00:33` 而这些代理软件做的只是把这套机制利用起来
`00:37` 所以当你打开系统代理这个开关的时候
`00:40` 主要发生了两件事
`00:42` 第一步监听端口
`00:44` 代理软件会在本机启动一个代理服务
`00:47` 并且监听一个端口
`00:49` 你可以把它理解成
`00:51` 他先在本机开了一个专门接收流量的入口
`00:55` 也就是说
`00:56` 代理软件已经在你的电脑上
`00:58` 把接流量的门打开了
`01:01` 但到这一步为止
`01:02` 虽然端口已经监听了
`01:04` 流量还没有真正转到这里
`01:06` 第二步
`01:07` 修改系统代理
`01:09` 设置监听端口之后
`01:11` 代理软件就借助了操作系统自带的代理能力
`01:15` 自动把系统代理功能打开
`01:17` 并去修改操作系统里的代理设置
`01:21` 把代理地址和自己监听的端口写进去
`01:24` 这样支持系统代理的软件
`01:27` 就知道要把网络请求交给代理软件处理
`01:31` 问题来了
`01:32` 为什么开了系统代理
`01:34` 有些软件还是没法走代理
`01:36` 这里就涉及到一个基础网络知识
`01:39` 网络传输大概可以分为四层
`01:42` 应用层
`01:43` 传输层
`01:44` 网络层
`01:45` 网络接口层
`01:47` 流量发出去的时候
`01:49` 大致是从上往下走的
`01:51` 最上面是应用层
`01:53` 比如我们浏览网页时常见的HTTPHTTPSS的请求
`01:59` 再往下是传输层
`02:01` 再往下是网络层
`02:03` 最底下是网络接口层
`02:05` 而系统代理主要影响的是比较上层的请求
`02:09` 尤其是HTTPHTTPS这一类应用层请求
`02:15` 所以像浏览器这种软件
`02:17` 通常会读取系统代理设置
`02:20` 然后把请求交给代理软件
`02:22` 但很多软件不一样
`02:24` 有些软件不会读取系统代理设置
`02:27` 或者他自己通过传输层已经出去了
`02:31` 这样一来
`02:32` 他的流量就不会进入代理软件监听的端口
`02:36` 系统代理就失效了
`02:38` PUN模式
`02:39` PUN模式开启后比较霸道
`02:42` 直接从最底层的网络接口层接管了所有流量
`02:47` 它怎么实现的
`02:48` 主要通过两个动作
`02:50` 第一创建一张虚拟网卡
`02:53` 你可以把它理解成
`02:54` 系统里多了一条虚拟的网络通道
`02:58` 第二修改路由规则
`03:00` 优先级
`03:01` 让原本要从真实网卡直接出去的流量
`03:04` 先进入这张虚拟网卡
`03:07` 流量进入虚拟网卡之后
`03:09` 代理软件就可以根据规则来判断哪些流量直连
`03:13` 哪些流量走代理
`03:15` 哪些流量需要分流
`03:17` 这就是为什么有些软件不支持系统代理
`03:21` 但打开tun模式之后
`03:23` 他就可以正常走代理
`03:25` 所以一句话总结
`03:26` 系统代理是在比较上层提供一个代理入口
`03:31` 让支持系统代理的软件主动把流量交过来
`03:35` TUN模式是在更底层
`03:38` 通过虚拟网卡和路由规则把流量先接管过来