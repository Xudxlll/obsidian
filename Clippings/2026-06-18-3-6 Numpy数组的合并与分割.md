---
title: "3-6 Numpy数组的合并与分割"
url: "https://www.bilibili.com/video/BV1T2X8YBEMK/"
bvid: "BV1T2X8YBEMK"
cid: "29025043442"
author: "黑梦sky"
upload_date: "2025-03-23"
subtitle_lang: "中文"
created: "2026-06-18"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=114210646264209&bvid=BV1T2X8YBEMK&cid=29025043442&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 字幕

`00:00` 我们在这一小节介绍NPY中
`00:03` 数组或者说是矩阵的合并与分割
`00:06` 这两个基本操作
`00:08` 那么对于这两个基本操作
`00:09` 虽然在我们的这个课程中可能使用的并不多
`00:13` 但是大家在机器学习算法准备数据的过程中
`00:17` 会经常地使用这两个操作
`00:19` 与此同时呢
`00:20` 出于我们介绍这个NPY相对的一个完整性
`00:24` 在这里将这两个非常重要的操作介绍一下
`00:27` 那么为了介绍这两个操作
`00:29` 我们先设置两个是一维的数组
`00:32` 同学们可以看
`00:33` 现在这个X就是一个有三个元素的一维向量
`00:37` Y是另外一个有三个元素的一维向量
`00:40` 在这种情况下
`00:41` 有一个非常普遍的操作
`00:44` 就是我想让X和Y这两个向量进行一个合并
`00:49` 现在X是一个13的向量
`00:51` Y是一个13的向量
`00:53` 我想获得一个1×6的向量
`00:55` 应该怎样做呢
`00:56` 其实非常的简单
`00:58` 在NP下面点有这样的一个方法叫做CONCATNT
`01:03` 那么有一些同学可能熟悉其他的语
`01:06` 言中的一些标准
`01:07` 比如说对于字符串的操作也有相应的这个方法
`01:11` 那么有的时候呢可能会简写成contact
`01:14` 其实就是拼接的意思
`01:16` 那么在CONCINNATE这个函数中
`01:19` 传入的这个参数可以是一个list
`01:23` 在list中列入我们想要合并的这两个向量
`01:29` 相应的这个变量
`01:30` 比如说在这里
`01:31` 我们就在这个方括号中写入X逗号Y
`01:35` 这样得到的结果
`01:36` 同学们可以看到就是另外一个ray
`01:39` 它是一个有六个元素的向量
`01:41` 将原先两个有三个元素的向量拼接了起来
`01:45` 在这里值得一提的是
`01:47` 我们的CONCINNATE这个列表中可以有多个元素
`01:51` 而不局限于只有两个元素
`01:53` 在这里再举一个例子
`01:55` 比如说我们再创建一个array
`01:57` 这个array呢我直接使用这种方式传入一个列表
`02:01` 来创建
`02:02` 这个array呢有三个666
`02:04` 我管它呢叫做Z
`02:05` 那么现在我们就可以调用NP点contact tnt
`02:09` 在这个列表中传入XYZ3个元素
`02:14` 这样就得到了一个含有九个元素的列表
`02:17` 非常的方便
`02:18` 那么现在我们尝试一下
`02:20` 基于二维数组或者说是矩阵进行一个操作
`02:24` 那么在这里呢我创建这样的一个矩阵A
`02:27` 对于矩阵A是23
`02:30` 一共有六个元素
`02:31` 那么我们尝试一下调用NP点concatenate1侧
`02:35` 在这个方括号中写两个A看看结果是什么
`02:40` 大家可以看结果是这个样子
`02:43` 变成了一个4×3这样的矩阵
`02:47` 简单的理解
`02:48` 同学们可以理解成原先这个数组中有两个元素
`02:53` 每个元素都是一个
`02:54` 含有三个元素的这样的一个数组
`02:57` 那么这样拼接之后就变成了有四个元素
`03:01` 每个元素依然是有三个元素这样的一个数组
`03:05` 那么相应的这样的拼接同学们可以理解成
`03:09` 比如说原来的数据A里有两个样本
`03:12` 每个样本有三个特征
`03:14` 现在又来了另外的两个样本
`03:17` 这两个样本呢依然是有和原来一样的三个特征
`03:21` 现在加起来我们一共有四个样本了
`03:23` 我们怎么把原来的两个样本
`03:26` 和后来的两个样本合在一起呢
`03:28` 我们就可以使用这样的方式
`03:30` 不过还存在这样的一种情况
`03:33` 就是原来有两个样本
`03:34` 这两个样本有三个特征
`03:36` 但现在新来了一个矩阵
`03:38` 这个矩阵表达的是原来的那两个样本
`03:42` 另外的三个特征
`03:44` 现在我要想让这两个矩阵拼接在一起
`03:47` 同学们可以想象一下
`03:48` 我们就要得到一个2×6的矩阵
`03:51` 那么在这种时候
`03:53` NP点concatenate也是支持这个操作的
`03:56` 只不过我们除了传入AA这样的两个
`04:01` 带拼接的矩阵之外
`04:02` 我们还需要传入另外一个参数
`04:07` 就是轴
`04:08` 那么默认这个轴是零
`04:10` 也就是沿着第一个维度的方向
`04:13` 第零个维度的方向进行拼接
`04:16` 那么结果就是原来有两行
`04:18` 现在变成四行
`04:20` 但是如果我们把这个轴设置为一的话
`04:23` 就是沿着第二个维度
`04:26` 也就是沿着列的方向进行拼接
`04:29` 那么同学们可以看一下
`04:30` 这样拼接
`04:31` 最终我们就获得了一个2×6
`04:34` 这样的一个矩阵
`04:35` 这就是沿着两个不同的方向进行拼接
`04:40` 在这个课程中
`04:41` 我们处理的数据最多就是一个二维的数组
`04:45` 也就是说是一个表格的形态
`04:47` 我们不会处理三维的数组这种情况
`04:52` 我们也可以填更高的维度
`04:54` 只要你的数据支持更高的维度
`04:57` 这一点呢同学们知道就可以了
`04:59` 那么现在我们有了concatenate这样的一个方法
`05:02` 我们还可以再做一些实验
`05:04` 比如说现在对于这个A是一个223
`05:08` 这样的一个矩阵
`05:09` 而对于这个Z呢
`05:11` 同学们可以看它是一个一维的数组
`05:13` 里面呢只包含三个元素
`05:16` 我们可以想象A这个矩阵表示有两个样本
`05:21` 这两个样本每个样本有三个特征
`05:25` 而现在呢Z这个向量表示又来了一个样本
`05:28` 这一个样本只有它本身的三个特征
`05:31` 所以它不是一个矩阵
`05:33` 它是一个向量
`05:34` 那么在这种情况下
`05:35` 我也想将Z表达的这一个样本
`05:38` 融合进A的这个矩阵中
`05:41` 那么在这种情况我们应该怎样做呢
`05:43` 可能有些同学已经想到了
`05:45` 可以尝试使用NP点concatenate
`05:48` 在这里头传入方括号中
`05:51` 首先有一个A
`05:52` 其次再来一个Z
`05:54` 这样是不是就可以了呢
`05:56` 我们运行一下会发现起是报错的
`05:59` 这个错误的原因是
`06:00` 这个大A和这个小Z他们的为数不一样
`06:05` 也就是说CONCATIENT只能处理为数一样的情况
`06:09` 现在我们的大A是一个二维矩阵
`06:11` 小Z是一个一维的向量
`06:13` 所以他们不能进行这种拼接
`06:15` 那么如果我们一定要进行这种拼接
`06:18` 一个做法就是将我们的这个小Z
`06:21` 同样也转为一个二维的矩阵
`06:24` 也就是把它从一个有三个元素的向量给写成
`06:29` 是一个1×3这样的二维矩阵
`06:32` 那么相信同学们就了解了
`06:33` 我们可以使用reshape这种方法
`06:36` 其中第一个维度只要一个元素
`06:38` 就只有一行
`06:39` 第二个维度呢我们传入一就是有多少个元素
`06:43` C多少个元素就可以了
`06:44` 那么这样我们就成功的可以
`06:47` 将小Z这个向量的数据
`06:49` 和原来的这个大A的矩阵进行一个连接操作
`06:54` 在这里同学们还要注意
`06:56` 这个连接操作的结果是一个新的矩阵
`07:00` 那么此时我们的A仍然是R乘以三的
`07:03` 所以如果我们要想保存拼接后的结果
`07:07` 我们需要把这个连接的结果给附上一个新的值
`07:12` 比如说我管它叫做A2
`07:15` 然后这样来写
`07:16` 那么此时A2就是我们这个拼接后的结果
`07:20` 那么在这种时候
`07:21` 可能有些同学会觉得我们使用这种concatenate
`07:25` 对于这样的小Z我们还需要使用reshape
`07:28` 是不是太麻烦了
`07:29` 其实NPY为我们提供了一个简便的方式
`07:34` 这个方式呢叫做v stack
`07:36` 那么这个V其实就是vertical的简写
`07:39` 就是在垂直方向
`07:41` stack就是将这个数据叠在一起
`07:44` 那么VISTACK这个方法它的容错性更好
`07:48` 对于里面的这个列表传入的两个数组
`07:53` 它可以非常智能的判断出来
`07:55` 怎样在垂直的方向进行叠加
`07:59` 即使它们的维度是不同的
`08:02` 那么我们可以尝试一下
`08:03` 在v stack中传入大和小Z这样的两个数组
`08:07` 虽然一个是二维数组
`08:08` 一个是一维数组
`08:09` 但是运行的结果依然可以
`08:11` 正确的把它们拼接在一起
`08:14` 那么相信聪明的同学也已经想到了
`08:17` 既然有MISTACK
`08:18` 就一定有h stack
`08:20` 也就是在水平的方向进行这种数据的堆叠
`08:24` 在这里呢我们做一个实验
`08:26` 我们设立一个叫做B的矩阵
`08:28` B的矩阵呢
`08:29` 我们用NP点four这个方法创建一个22
`08:34` 其中每一个元素呢都是100这样的一个矩阵
`08:37` 那么B这个矩阵就是这个样子
`08:40` 此时我们就可以使用NP点h stack
`08:44` 来尝试
`08:45` 将A和B这两个矩阵在水平方向堆叠在一起
`08:51` 同学们可以想象一下
`08:52` 由于A它是一个23的矩阵
`08:55` B呢是一个22的矩阵
`08:58` 所以这两个矩阵在水平方向摞在一起
`09:02` 最终的结果就是这样的一个2×5的矩阵
`09:05` 非常的简单
`09:06` 那么当然啦
`09:07` 在这里我说v stack和h stack可以自动的转换
`09:12` 传入的这个参数
`09:14` 传入的这个维度
`09:15` 但这不代表如果我们传入一个非法的数组
`09:18` 它也可以帮助我们进行正确的合并
`09:21` 举一个例子
`09:23` 比如说我们的A是一个23
`09:25` 这样的一个矩阵
`09:27` 我们的小Z是一个13在这里的一个向量
`09:31` 那么显然
`09:32` 我们在竖直方向可以将这两个数组堆叠在一起
`09:37` 但是在水平方向是无法堆叠在一起的
`09:40` 那么在这里呢我们就可以实验一下
`09:42` 我们调用NP点h stack
`09:44` 在里面方括号中传入大A和小Z运行这个程序
`09:49` 同学们就会发现编译器是报错的
`09:52` 那么这就是数据的合并
`09:55` 它的使用方式
`09:57` 下面和数据的合并是反向的操作
`10:01` 我们来看一下NPY中的分割
`10:04` 这样的一个操作依然是首先我们创建一个向量
`10:09` 是一维的向量
`10:10` 它有十个元素
`10:11` 从0~9
`10:12` 这样写NP有一种方法叫做split
`10:16` 这个方法传入两个参数
`10:18` 第一个参数就是我们要分割的那个数组对象
`10:22` 在这里呢我们想对小X进行分割
`10:25` 而第二个参数是我们的分割点
`10:29` 那么它传入的也是一个列表
`10:32` 我们可以在这个列表中指定多个分割点
`10:35` 比如说在这里我指定两个分割点
`10:37` 一个是三
`10:38` 一个是七
`10:39` 那么对于一个数组
`10:40` 如果我指定了两个分割点
`10:42` 如果这两个分割点都是合法的话
`10:44` 我们其实就将这个数组切成了三段
`10:48` 那么它的返回值就是分别这三段的数组
`10:51` 在这里呢我们就可以尝试一下
`10:53` 将这三个内容分别付给X1X2X三
`10:57` 我们这样来写
`10:58` 我们就可以看一下此时X1是谁
`11:02` 大家可以看是012
`11:04` 也就是说我们第一个分割点是三
`11:07` 所以和数据的切片一样
`11:09` 相当于是从零一直到三三
`11:12` 不包含这些数据
`11:13` 形成了第一部分
`11:15` 我们再来看X2是谁
`11:17` X2对应的就是3456
`11:20` 其实就是三冒号七这部分内容
`11:23` 而相应的X3对应的就是七冒号
`11:26` 一直到结尾相应的内容
`11:28` 那么这就是数据的分割
`11:30` 对于数据的分割而言
`11:32` 我们这是将整个数据切成了三段
`11:35` 所以我们传进一这个数组
`11:37` 这个数组中有两个元素
`11:39` 那么如果我们想将整个数据切割成两段的话
`11:44` 我们传的第二个参数依然也要是一个数组
`11:48` 只不过这个数组中只需要有一个元素就好了
`11:52` 比如说我以五作为分割点
`11:54` 那么我们就将这个数组分成了两段
`11:57` 我们管它叫X1和X2
`11:59` 这样执行以后
`12:00` X1同学们可以看就是01234这五个元素
`12:04` 而X2呢就是56789这五个元素
`12:08` 那么我们之前对这个数组进行分割
`12:11` 使用一维数组为例
`12:13` 那么和合并操作一样
`12:15` 对于二维数组是同样的
`12:17` 在这里呢我创建了一个二维的矩阵
`12:20` 同学们可以看我使用这种方法
`12:22` 首先创建了一个从0~15这样的一维的向量
`12:26` 然后用reshape的方法
`12:28` 将它转换成了一个4×4的二维的矩阵
`12:32` 叫做大A那么现在对于这个大A
`12:35` 我也可以使用NP点split
`12:38` 对于大A我们分成两部分
`12:41` 那么在这里呢我传入一个二
`12:44` 然后得到的结果我管它叫做A1和A2
`12:48` 我们来看看这样得到的结果
`12:50` A1是什么样子的呢
`12:51` A1是上面的两行
`12:54` 而相应的A2就是下面的两行
`12:58` 所以在这里同学们再次看到了
`13:00` 当我们从一维转换到二维的时候
`13:03` 永远有一个基于哪个维一度进行分割
`13:07` 或者进行合并这样的问题
`13:09` 那么在默认的情况下
`13:11` 我们依然是基于行所在的这个维度
`13:14` 也就是所谓的第零个轴
`13:16` 就是第一个维度进行分割
`13:18` 所以我们是在行这个方向上
`13:21` 上两行和下两行分割成了两个矩阵
`13:25` 那么我们可不可以在列的方向进行分割呢
`13:28` 当然可以
`13:29` 相信很多同学已经想到了
`13:31` 和前面一样
`13:31` 我们只需要传入另外一个参数
`13:38` 如果我们传承是一的话
`13:41` 那么此时我们再来看我们的A1
`13:43` 就变成了一个4×2
`13:45` 这样的矩阵相当于原来A这个矩阵的左半部分
`13:49` 而相应的A2就是另外一个42的矩阵
`13:53` 就是原来的A这个矩阵的右半部分
`13:56` 那么依然是和之前讲的合并一样
`13:59` 可能有些同学觉得传xis这样的一个参数
`14:03` 不够的直观
`14:04` 所以那么PY为我们设置了两个新的方法
`14:08` 第一个方法叫做v spit
`14:10` 又是顾名思义
`14:11` 同学们肯定就知道了
`14:13` 它是在垂直的方向进行分割
`14:16` 也就是vertical split
`14:17` 那么在这里我们只需要传入两个参数就好了
`14:20` 我们要对A在垂直的方向
`14:23` 以二这个点作为分割点进行一次分割
`14:27` 那么同学们可以想象在垂直方向进行分割
`14:30` 分割的结果就是上下两部分
`14:32` 所以呢我把这两部分叫做upper和lower
`14:35` 那么这样执行之后
`14:37` 我们可以看一下
`14:38` 阿普相应的就是上半部分的矩阵
`14:41` 而lower相应的是下半部分的矩阵
`14:44` 那么同理就有一个对称的方法叫做h spit
`14:49` 也就是在水平方向进行分割
`14:51` 我们依然对A进行以二这个点为分割点的水平
`14:57` 方向进行分割
`14:58` 同学们可以想想象一下
`14:59` 最后这个分割的结果就是左右两个子矩阵
`15:03` 我们管它呢叫做left和right
`15:05` 那么这样执行完以后
`15:07` left对应的就是42的左半边的矩阵
`15:11` 而right就是42的右半边的矩阵
`15:14` 对于这些方法
`15:16` 同学们可以根据自己的喜好来选择合适的方法
`15:20` 最后值得一提的是
`15:21` 我们在这里讲的是数据的分割
`15:24` 那数据的分割有什么意义呢
`15:26` 那么一个最典型的意义
`15:28` 同学们回忆一下
`15:29` 我们之前讲机器学习对于这一组数据而言
`15:34` 大家读进来的数据
`15:35` 如果是一个4×4的矩阵
`15:38` 它很有可能代表的是
`15:39` 首先我们是有四个样本
`15:41` 对于每个样本我们有三个特征
`15:44` 那么前三列分别代表三个不同的特征
`15:47` 而最后一列代表的是我们每每一个样本对应的
`15:51` 那个label的值
`15:52` 也就是我们的目标值
`15:54` 它可能是一个类别
`15:55` 也可能是一个连续的数字
`15:57` 比如说表示它是猫还是狗
`15:59` 或者表示房子的价格
`16:01` 最终这个价格具体是多少
`16:03` 那么我们在运行我们机器学习算法的时候
`16:06` 就要将这个特征矩阵和这个label向量给分隔开
`16:11` 那么在这种情况下
`16:12` 我们就可以这样做
`16:13` 我们最终分隔开是一个大的X和一个小的Y
`16:17` 让它等于NP点
`16:19` 我们是在水平的方向进行分割
`16:21` h spit传入
`16:23` 首先DA是我们带分割的矩阵
`16:25` 然后我们的分割点在这里
`16:28` 我就传入一个一
`16:29` 就代表我只是想把最后的那一列给拿下来
`16:34` 我们只需要这样写
`16:35` 然后执行之后
`16:36` 同学们再看
`16:37` 此时这个X就是一个43的矩阵
`16:41` 而这个Y相应的变成了一个41的矩阵
`16:45` 当然了
`16:45` 有些时候
`16:46` 我们希望我们获得的这个Y是一个向量
`16:50` 那么在这种情况下呢
`16:51` 同学们可以再回忆一下
`16:53` 我们可以把这个4×1的矩阵
`16:56` 把它唯一的这一列给抽出来
`16:58` 抽出来的方法就是用这种方括号索引的方式
`17:02` 在行这个维度抽出所有的行
`17:05` 而在列这个维度呢只针对第零列抽出来
`17:09` 那么最终得到的这个结果就变成了一个向量
`17:13` 那么在这里我用一个不是真实的例子
`17:16` 随便找了一个矩阵
`17:17` 只是说明了split可能使用的一个场景
`17:22` 那么之后同学们在处理自己的数据的时候
`17:25` 通常是一个矩阵
`17:27` 有可能在其他的时候
`17:29` 也需要将这个矩阵切割成几个不同的部分
`17:33` 对这几个不同的部分进行分别处理之后
`17:36` 再将它们合并起来
`17:38` 那么依然是在我们的这个课程中
`17:41` 其实对数据的预处理不会有那么复杂
`17:43` 但是我个人认为对于这个矩阵的合并和分割
`17:47` 是两个非常重要的操作
`17:49` 希望同学们也能够掌握
`17:51` 那么这一小节就到这里
`17:53` 我们在下一小节将开始介绍
`17:55` NPY所支持的矩阵的运算
`17:58` 大家加油