---
title: "激活函数迭代史：SwiGLU 到底强在哪？居然连作者自己都说不清？"
url: "https://www.bilibili.com/video/BV1yGMc6aEU6/"
bvid: "BV1yGMc6aEU6"
cid: "39639518324"
author: "算法魔法师"
upload_date: "2026-07-07"
subtitle_lang: "中文"
created: "2026-07-07"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=116859986777674&bvid=BV1yGMc6aEU6&cid=39639518324&page=1&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

对于论文最后作者的说法，你们怎么看呢？如果这期视频对你有帮助的话，麻烦帮我一键三连支持一下，感谢铁铁们～

## 字幕

`00:00` 你有没有想过激活函数换了一波又一波
`00:02` 从最早的sing mod
`00:04` 再到后面一统天下的re录
`00:05` 再到现在q n deep sic全都在用的switch g l u
`00:08` 这中间发生了什么
`00:10` 这期视频我将带你从sick mod开始
`00:12` 到现在最火的SWEGLU
`00:14` 把这一整个技术路线全部给你串一遍
`00:17` 我敢说
`00:17` 这是全网
`00:18` 把这段迭代路线讲的最清楚的一期视频
`00:21` 可以先点赞收藏
`00:22` 交个学费
`00:22` 我们现在开始
`00:26` 首先登场的就是这个spring boy
`00:28` 最早期的S型激活函数
`00:30` 代表
`00:30` 1838年呢
`00:31` 由这个比利时数学家阿拉VERA首次提出
`00:34` 并于1986年被广泛利用
`00:36` 在神经网络反向传播中
`00:38` 当作激活函数中使用
`00:39` 它的图像呢是这个样子的
`00:41` 取值范围呢在0~1之间
`00:42` 非常适合用来做二分类
`00:44` 并且易于求导
`00:45` 可以看到它的导数呢就自己乘上一减掉自己
`00:48` 但是最大的问题也是
`00:49` 因为导数最大值呢仅0.25
`00:51` 在早期电脑性能较差的时候
`00:53` 神经网络都比较浅
`00:54` 所以训练上不会造成太大的干扰
`00:56` 但随着现在电脑性能愈发强大啊
`00:59` 网络也越来越深
`01:00` 导致在反向传播的时候呢
`01:01` 因为每层都呈上一个0.25啊
`01:03` 不断的缩小
`01:03` 从而极易出现梯度消失的现象
`01:05` 导致无法完成深层网络的一个训练
`01:08` 然后就是ten h了
`01:09` 被作为singing mod的一个改款
`01:11` 你可以把它理解成了一个以零为中心的single mod的
`01:14` 版本
`01:14` 的取值范围呢
`01:15` 从史克摩尔的0~1变成了-1~1
`01:18` 中心点呢正好在零点这个调整让它实现了输出
`01:21` 以零为中心
`01:22` 相对SMOD的输出永远是正的来说
`01:24` 训练相对更稳定一些
`01:26` 并且最关键的一点是NH的导数范围更大
`01:29` 最大可以达到一
`01:30` 相比SIMONE的每升一层层最多0.25来说
`01:33` 大幅减缓了浅层网络的一个梯度消失的问题
`01:36` 一度成为早期的CNNNR
`01:37` NNALSTM的首选激活函数
`01:40` 但是它仍然没有摆脱梯度消失的这个问题
`01:42` 因为它下去的导数最大值也只有一嘛
`01:44` 在生成网络的反向传播过程中呢
`01:47` 多层梯度连城之后
`01:48` 数值依然会指数级衰减
`01:49` 一旦网络层数超过啊
`01:51` 几十层模型依旧会出现训练停滞等问题
`01:54` 那正是因为sim mod和TNH的轻度缺陷
`01:57` 一个看起来非常简单
`01:58` 粗暴无脑的激活函数出现了韦录
`02:00` 它的雏形呢其实在1969年就已经出现了
`02:03` 但是早期并没有受到重视
`02:04` 直到2012年的alex net上面
`02:07` 水陆作为卷积网络的激活函数
`02:08` 几乎彻底解决了激活函数导致的
`02:11` 生成网络梯度消失的问题
`02:12` 训练速度远超turn h
`02:14` 还有thing mod
`02:15` 吕布呢
`02:15` 从此成为了CNN呢深度神经网络标配的激活函数
`02:19` 彻底改写了深度学习的发展现状
`02:21` 直到现在依旧有大批的模型在使用它
`02:23` 它的公式非常简单
`02:24` 输入大于零的部分
`02:26` 原样输出小于零的部分呢
`02:27` 自为零
`02:28` 这种简单的计算逻辑呢
`02:29` 使得它的运算速度也远超了SINGMOD和ten h
`02:32` 大幅的降低了模型训练的算力消耗
`02:34` 最关键的一点是
`02:35` 当输入为正数的时候
`02:37` 它的导数恒为一
`02:38` 在反向传播中
`02:39` 梯度永远不会衰减
`02:41` 基本上解决了梯度消失的问题还是非常厉害的
`02:44` 但雨露呢也并非完美无缺
`02:46` 最典型的问题就是神经元死亡
`02:48` 如果参数更新不让
`02:49` 导致神经元输入啊长期小于零
`02:51` 那么这个参数的梯度就会永远为零
`02:53` 从而使得该神经元彻底失效
`02:55` 不再参与任何参数的更新
`02:57` 造成模型特征提取性能的永久损失
`02:59` 除此之外呢
`03:00` REVIE的负区间梯度完全截断
`03:02` 也会丢失一定的有效信息
`03:03` 因为负数本身也是一种信息嘛
`03:06` 为了修复reload这个问题呢
`03:07` 水路和举路这两个激活函数又应运而生
`03:10` 先说水路吧
`03:11` 是switch的一个特例
`03:12` 看他的公式
`03:13` 它是SIGMOID和瑞录的一个结合体
`03:15` 它将SIGMOID作为一个门控函数接入
`03:18` 既保留re录的正向线性的特性
`03:20` 同时在小于零的部分呢
`03:22` 又用平滑曲线替代RL的硬阶段
`03:24` 也就在负数区间并不是直接置零了
`03:26` 而是会稍微保留一咩咩的梯度信息
`03:29` 那什么叫门控函数呢
`03:30` 你看这里
`03:31` 输入X每次激活时都会和SIGMOID的相乘
`03:34` SIGMOID就是一个门控开关控制信息通过的比例
`03:37` 我们知道
`03:37` SIGMOID是一个区间为0~1的一个函数
`03:40` 假设当输入X为三的时候
`03:41` SIGMODE的X就约为0.95
`03:43` 再乘上原本的这个三之后呢就约为2.85
`03:46` 相比较三来讲
`03:47` 丢失的信息较少
`03:48` 而当X等于三的时候
`03:50` SIGMODEX约为0.05
`03:52` 最后输出呢约为负的0.15
`03:54` 也保留了少量赋值信息
`03:56` 从而较好地解决了李璐神经元死亡的一个问题
`03:59` 并且呢它全程可导
`04:00` 不像REWRT在零点处不可导
`04:02` 像优路系列模型
`04:03` 在很多代里头均使用这个CU激活函数
`04:06` 而举露呢则是跟水路类似
`04:08` 只不过它使用的是标准正态分布的累积分布
`04:11` 作为门控函数
`04:12` 也就是个FX
`04:13` 这图像呢看起来跟C路非常相似
`04:16` 但是在副区间内保留的信息更少一些
`04:18` 那什么是标准正态分布的累积分布呢
`04:21` 其实就是这个正态分布
`04:22` 从负无穷到当前点X的积分
`04:24` 这么讲有点抽象
`04:26` 这个呢是正态分布的图像
`04:28` 典型的中型曲线对吧
`04:29` 那标准正态分布的累积分布呢
`04:31` 就是从负无穷到X点下的正态分布的面积
`04:34` 也就这一部分的面积
`04:36` 比如说我们的X点在零点处
`04:38` 那它的面积就是从左半边
`04:39` 一直到零点的部分的面积
`04:41` 比如说X在零处
`04:43` 那这个累积分布
`04:44` 就是从负半轴到零点部分的面积
`04:46` 所以这个FX的区间范围呢就在0~1之间
`04:49` 因为你面积最大也就百分百嘛
`04:51` 输入的X越大
`04:52` 经过这个FX后
`04:53` 输出的值就越靠近
`04:54` 一表示面积越大嘛
`04:55` 输入X越小呢
`04:57` 输出的就越靠近零
`04:58` 同样也起到了个门控的作用
`05:00` 这个激活函数在早期的博尔特
`05:01` 还有GPT上都被广泛的应用了
`05:03` 最后就是当下大模型最热门的激活函数了啊
`05:06` swing gl u无论是国外的拉玛系列还是国内的啊
`05:10` Q问呐
`05:10` deep sk等系列
`05:11` 都全面选择了spring glu作为他们的计划函数
`05:14` 这个spring gl u全称叫做switch门控线性单元
`05:17` 和上述的那些激活函数不同
`05:19` 它并不是简单的输入一个数
`05:21` 输出一个数
`05:22` 而是有点类似transformer里面的
`05:23` FFN层的一个改造
`05:25` 这个呢是FFN层的一个执行流程
`05:28` 假设当前token的特征是这个
`05:29` 那么它会先经过一个线性层放大维度
`05:32` 然后经过re录举入这种激活函数
`05:34` 最后再通过一个线性层降维回原来的大小
`05:36` 而使命GLU呢
`05:37` 则把这一部分改成了两个分支的并行
`05:40` 分别成为了两个参数矩阵
`05:42` 这条呢负责生成内容
`05:43` 得到一组特征
`05:44` 而这条呢则是先经过刚刚说的水路
`05:47` 来当成一个门控开关
`05:48` 最后再将这两部分逐元素相乘
`05:50` 然后乘上一个线性层缩放回原来的一个维度
`05:53` 比如说这条分支里某个位置输出了十
`05:56` 可能代表一个很强的特征
`05:58` 但另外一条门控分支却给他一个0.1
`06:00` 那么相乘之后呢
`06:01` 他就只剩下了一说明模型虽然发现了这个特征
`06:04` 但门控就觉得他不太重要
`06:05` 所以传统的瑞录啊
`06:07` 举录啊
`06:07` 更像是每个特征自己决定要不要保留
`06:09` 那这个spring gl u呢
`06:11` 这是专门使用了两条由不同参数矩阵生成的
`06:14` 特征分支来独立判断哪些特征更重要
`06:17` 从而具备了更强的特征筛选能力
`06:19` 而且关于这个swing gl u还有一个很搞笑的点
`06:22` 在论文的结尾部分呢
`06:23` 作者说这些架构到底为什么这么有效
`06:25` 他们也没有一个完整的理论解释
`06:27` 甚至说这一切都归功于神的恩赐
`06:30` 对此你怎么看呢
`06:31` OK如果这期视频对你有帮助的呢
`06:33` 不妨点赞收藏转发一波
`06:35` 咱们下期视频见